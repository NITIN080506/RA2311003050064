{
  "name": "logging-middleware",
  "version": "0.1.0",
  "description": "Reusable logging middleware that posts logs to the test server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "example": "ts-node src/example.ts"
  },
  "keywords": ["logging", "middleware", "logger"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "@types/node": "^20.3.1"
  }
}
